@AGENTS.md

## Commit Rules

- Do NOT include `Co-Authored-By: Claude` or any Claude attribution in commit messages.
- After every push, always create a PR to `main` using `gh pr create`.
- Always run the affected tests after making changes and verify they pass before committing.
